# 마케팅 초이스 제작 기록

## 진행과정

- 단 하나의 HTML 파일로 소상공인용 진단 게임인 **사장의 마케팅 게임**을 만들기로 했다.
- 15개 질문과 시작·결과 화면을 연결하고, 답변마다 7개 마케팅 유형에 복합 가중치(한 답이 여러 유형에 점수를 주는 방식)를 적용했다.
- 결과 화면에 11개 도구의 우선순위, 필수·선택 마케팅, 낮은 우선순위, 첫 미션, 선택 횟수와 엔딩 이름을 차례로 붙였다.
- 고구마켓 로고를 HTML 안에 Base64(이미지를 문자 데이터로 바꾸어 파일 안에 넣는 방식)로 삽입하고 첫 화면과 마지막 화면에 배치했다.
- 이후 제목·제작자 표기, 기록 자동 저장, 누적 엔딩 순위표, 다시하기 버튼과 화면 정렬을 순서대로 다듬었다.

## 결과 및 주요판단

- 결과는 한 가지 유형만 보여주지 않고 상위 유형 조합과 7개 마케팅 DNA를 함께 보여주기로 했다.
- 모든 도구를 권하지 않고, 지금 필요한 도구와 당장 하지 않아도 되는 도구를 나누어 보여주기로 했다.
- 기록은 localStorage(브라우저 안에 데이터를 계속 보관하는 저장 공간)에 최대 200개까지 저장하고, 7개 엔딩의 누적 도달 횟수로 순위를 만들었다.
- 작업 중 Windows sandbox helper 오류(파일 접근을 중계하는 기능의 오류)로 읽기·패치·미리보기가 여러 번 실패했다.
- 이후 승인된 PowerShell 파일 접근과 정확히 한 번 일치할 때만 바꾸는 검증식 치환으로 수정했으며, JavaScript 문법 검사도 통과했다.

## 배운점/인사이트

- 마케팅 진단은 단순 점수보다 한 답변이 여러 유형에 영향을 주는 복합 가중치가 실제 사업 모습에 더 가깝다.
- 좋은 결과 화면은 “무엇을 할지”뿐 아니라 “지금 무엇을 하지 않아도 되는지”까지 알려줘야 한다.
- Base64를 쓰면 로고 같은 이미지도 HTML 하나에 담아 다른 파일 없이 실행할 수 있다.
- localStorage를 사용하면 새로고침하거나 브라우저를 다시 열어도 누적 결과 기록을 유지할 수 있다.
- 반응형 디자인(화면 크기에 맞춰 배치와 글자 크기가 바뀌는 방식)은 모바일에서 줄바꿈과 중앙 정렬을 따로 확인해야 한다.

## 다음 실습에 적용할 나만의 원칙

1. 처음 요청할 때 화면 제목, 정확한 문구, 필요한 기능, 최종 파일명을 한 번에 적는다.
2. 이미지가 필요하면 원본 파일을 함께 주고, 단일 파일 안에 포함할지 별도 파일로 둘지도 명확히 말한다.
3. 수정 요청은 “어느 화면의 무엇을 어떻게 바꿀지”와 새로고침 후에도 유지해야 할 데이터를 구체적으로 적는다.
